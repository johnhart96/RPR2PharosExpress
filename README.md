This is a simple Python script that will take triggers from contact closure on a Raspberry Pi and  auto-generate the correct UDP command to use to send a trigger to Pharos Express/Expert.
## Install Python
 ```sudo apt install python3 -y```
